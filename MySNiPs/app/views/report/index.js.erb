$("#cards").append('<%= j render(@cards) %>');
<% if @search %>
  $("#cards").html("<%= escape_javascript(render(@cards)) %>");
<% elsif @cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@cards) %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
