<%= stylesheet_link_tag "bootlogin" %>
<%= form_for :login do |f| %>
<div class="text-center">
    <form class="form-signin">
    <a href="/">
    <%= image_tag("logo.png", :style=>'height:75px;', :class=>'d-inline-block') %>
    </a>
    <%= f.text_field :identifier, autofocus: true, :class=>'form-control', :placeholder=>'Identificador' %>
    
    <%= f.password_field :password, :class=>'form-control mb-3', :placeholder=>'Password',:style=>'width:300px;' %>
	<small class="text-muted">Ao clicar em login, você aceita nossos </small><small class="text-info"><u><a href="#" data-toggle="modal" data-target="#termos_uso">termos de uso</a></u></small>
    <% if alert %>
      <p class="text-danger"><%= alert %></h1>
	<% end %>

    <% if current_user %>
    <%= link_to 'Log Out', logout_path, method: :delete %>
    <% end %>

    <%= f.submit 'Log In',:class=>'btn btn-lg btn-primary btn-block mt-3' %>
    
        
    </form>

</div>
<% end %>

<!-- Modal -->
<div class="modal fade" id="termos_uso" tabindex="-1" role="dialog" aria-labelledby="termos_uso" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Termos de Uso</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Fechar">
          <span aria-hidden="true" class="text-danger">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">Fechar</button>
      </div>
    </div>
  </div>
</div>