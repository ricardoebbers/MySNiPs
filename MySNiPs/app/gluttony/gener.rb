module Gluttony
  class GeneBuilder
    # Class that will get information out of blobs of text and an unorganized tables
    # Never will return a nil Gene, for Genes only exist when there is a valid Geno
    def initialize(rsnum, meta, raw)
      # do things
    end
  end
end
